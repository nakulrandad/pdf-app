# pdf-app

Web app to play around with probability density functions.

## Requirements

- Python version: **3.7**
- Other dependencies: Install using `requirements.txt`

## Setup Instructions

1. Create a virtual environment:
   ```bash
   python3.7 -m venv .venv
